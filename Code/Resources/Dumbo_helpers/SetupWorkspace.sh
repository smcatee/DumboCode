
# should be able to start with only .java and inputs and end with everything set for user to run hadoop immediately

# check if workspace is clean

# check for folders with .git repo, if able to update ask user if they want to

# make directories (make new folders based on program names)

# check that everything hadoop needs is present: input files or dirs, .jar of map/red/job, output location